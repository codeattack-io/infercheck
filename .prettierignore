# Build output
.next/
node_modules/

# Provider compliance data — written by scripts via JSON.stringify(x, null, 2),
# which always expands arrays. Prettier collapses short arrays onto one line, so
# formatting these would be undone by the next sync/research/translate run.
data/providers/

# Drizzle migrations and meta snapshots are generated by drizzle-kit.
# See AGENTS.md: never edit these by hand.
drizzle/
